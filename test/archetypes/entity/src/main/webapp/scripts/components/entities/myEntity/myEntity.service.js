'use strict';

angular.module('jhipsterApp')
    .factory('MyEntity', function ($resource) {
        return $resource('api/myEntitys/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
