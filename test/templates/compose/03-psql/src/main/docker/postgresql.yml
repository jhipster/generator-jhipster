version: '2'
services:
    mspsql-postgresql:
        image: postgres:9.5.1
        # volumes:
        #     - ~/volumes/jhipster/mspsql/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=mspsql
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
