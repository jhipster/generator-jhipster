version: '2'
services:
    mspsql-postgresql:
        image: postgres:13.3
        # volumes:
        #     - ~/volumes/jhipster/mspsql/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=mspsql
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
