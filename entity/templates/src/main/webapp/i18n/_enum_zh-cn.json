{
    "<%= angularAppName %>": {
        "<%= enumName %>" : {
            "SELECT": "{VALUE, select, <% for (enumId in enums) { %><%=enums[enumId]%>{<%=enums[enumId]%>} <% } %>other{}}"<% for (enumId in enums) { %>,
            "<%=enums[enumId]%>": "<%=enums[enumId]%>"<% } %>
        }
    }
}
