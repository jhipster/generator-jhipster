# ===================================================================
# Spring Boot configuration.
#
# This configuration will be overriden by the Spring profile you use,
# for example application-dev.yml if you use the "dev" profile.
# ===================================================================

# ===================================================================
# Standard Spring Boot properties.
# Full reference is available at:
# http://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
# ===================================================================

spring:
    jpa:
        open-in-view: false
        hibernate:
            ddl-auto: none
            naming-strategy: org.springframework.boot.orm.jpa.hibernate.SpringNamingStrategy
    messages:
        basename: classpath:/i18n/messages
    mvc:
        favicon:
            enabled: false
    thymeleaf:
        mode: XHTML
<%_ if (enableSocialSignIn) { _%>
    social:
        # see https://developers.google.com/+/web/signin/server-side-flow#step_1_create_a_client_id_and_client_secret
        google:
            clientId: 265902152073-t5btnbi1vjqiil0aadpgvq0v0c32j1r5.apps.googleusercontent.com
            clientSecret: YdB6HmeEAGZD_qNSHHtk0lCr

        # see https://developers.facebook.com/docs/facebook-login/v2.2
        facebook:
            clientId: 526986260792976
            clientSecret: d9a9dd358f7f56cab17f5a0f0b3f5179

        # see https://apps.twitter.com/app/
        twitter:
            clientId: Ur2i77DLVRfqJCZj592b1tG07
            clientSecret: Jyug5gSA3xLgEzERCzhAjjcUMyshCk2IOzpmns0515UvJSf2hS
<%_ } _%>

security:
    basic:
        enabled: false

# ===================================================================
# JHipster specific properties
# ===================================================================

jhipster:
    async:
        corePoolSize: 2
        maxPoolSize: 50
        queueCapacity: 10000
    mail:
        host: localhost
        port: 25
        username:
        password:
        protocol: smtp
        tls: false
        auth: false
        from: <%= baseName %>@localhost
    security:
<%_ if (authenticationType == 'oauth2' || authenticationType == 'xauth') { _%>
        authentication:
    <%_ if (authenticationType == 'xauth') { _%>
            xauth:
                secret: myXAuthSecret
                # Token is valid 30 minutes
                tokenValidityInSeconds: 1800
    <%_ } _%>
    <%_ if (authenticationType == 'oauth2') { _%>
            oauth:
                clientid: <%= baseName %>app
                secret: mySecretOAuthSecret
                # Token is valid 30 minutes
                tokenValidityInSeconds: 1800
    <%_ } _%>
<%_ } _%>
        rememberme:
            # security key (this key should be unique for your application, and kept secret)
            key: <%= rememberMeKey %>
    swagger:
        title: <%=baseName%> API
        description: <%=baseName%> API documentation
        version: 0.0.1
        termsOfServiceUrl:
        contact:
        license:
        licenseUrl:
<%_ if (enableSocialSignIn) { _%>
    social:
        redirectAfterSignIn: "/#/home"
<%_ } _%>
