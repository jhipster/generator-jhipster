# Application specific properties

# i18n resource files reloading 
message.reloading.enabled=${message.reloading.enabled}

# log configuration
logback.loglevel=${logback.loglevel}

# ehcache configuration 
ehcache.maxBytesLocalHeap=${ehcache.maxBytesLocalHeap}

# database configuration
datasource.driverclassname=${datasource.driverclassname}
datasource.url=${datasource.url}
datasource.username=${datasource.username}
datasource.password=${datasource.password}
datasource.initialSize=${datasource.initialSize}
datasource.minIdle=${datasource.minIdle}
datasource.maxIdle=${datasource.maxIdle}
datasource.maxActive=${datasource.maxActive}
hibernate.dialect=${hibernate.dialect}
hibernate.show_sql=${hibernate.show_sql}
hibernate.generate_statistics=${hibernate.generate_statistics}
