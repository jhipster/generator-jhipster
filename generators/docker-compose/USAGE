Description:
    Creates all required Docker deployment configuration for the selected applications.

Example:
    yo jhipster:docker-compose

    This will create:
        docker-compose.yml

    If using Service discovery and Configuration:
        jhipster-registry.yml or consul.yml
        central-server-config/application.yml

    If using ELK monitoring:
        elk.yml
        log-conf/logstash.conf
        log-data/
