<%#
 Copyright 2013-2026 the original author or authors from the JHipster project.

 This file is part of the JHipster project, see https://www.jhipster.tech/
 for more information.

 Licensed under the Apache License, Version 2.0 (the "License");
 you may not use this file except in compliance with the License.
 You may obtain a copy of the License at

      https://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing, software
 distributed under the License is distributed on an "AS IS" BASIS,
 WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 See the License for the specific language governing permissions and
 limitations under the License.
-%>
<div>
  <h2 id="page-heading" data-cy="<%- userManagement.entityNameCapitalized %>Heading">
    <span>__jhiTranslateTag__('userManagement.home.title')</span>

    <div class="d-flex justify-content-end">
      <button class="btn btn-info me-2" (click)="loadAll()" [disabled]="isLoading()">
        <fa-icon icon="sync" [animation]="isLoading() ? 'spin' : undefined" />
        <span>__jhiTranslateTag__('userManagement.home.refreshListLabel')</span>
      </button>
      <button class="btn btn-primary jh-create-entity" [routerLink]="['./new']" data-cy="entityCreateButton">
        <fa-icon icon="plus" />
        <span>__jhiTranslateTag__('userManagement.home.createLabel')</span>
      </button>
    </div>
  </h2>

  <<%= jhiPrefixDashed %>-alert-error />

  <<%= jhiPrefixDashed %>-alert />

@if (users()) {
  <div class="table-responsive">
    <table class="table table-striped" aria-describedby="user-management-page-heading">
      <thead>
        <tr<% if (!locals.databaseTypeCassandra) { %> <%= jhiPrefix %>Sort [(sortState)]="sortState" (sortChange)="transition($event)"<% } %>>
          <th scope="col"<% if (!locals.databaseTypeCassandra) { %> <%= jhiPrefix %>SortBy="id"<% } %>>
            <span>__jhiTranslateTag__('global.field.id')</span><% if (locals.databaseType !== 'cassandra') { %> <fa-icon icon="sort" /><% } %>
          </th>
          <th scope="col"<% if (!locals.databaseTypeCassandra) { %> <%= jhiPrefix %>SortBy="login"<% } %>>
            <span>__jhiTranslateTag__('userManagement.login')</span><% if (locals.databaseType !== 'cassandra') { %> <fa-icon icon="sort" /><% } %>
          </th>
          <th scope="col"<% if (!locals.databaseTypeCassandra) { %> <%= jhiPrefix %>SortBy="email"<% } %>>
            <span>__jhiTranslateTag__('userManagement.email')</span><% if (locals.databaseType !== 'cassandra') { %> <fa-icon icon="sort" /><% } %>
          </th>
          <th scope="col"></th>
<%_ if (enableTranslation) { _%>
          <th scope="col"<% if (!locals.databaseTypeCassandra) { %> <%= jhiPrefix %>SortBy="langKey"<% } %>>
            <span>__jhiTranslateTag__('userManagement.langKey')</span><% if (locals.databaseType !== 'cassandra') { %> <fa-icon icon="sort" /><% } %>
          </th>
<%_ } _%>
          <th scope="col"><span>__jhiTranslateTag__('userManagement.profiles')</span></th>
<%_ if (!locals.databaseTypeCassandra) { _%>
          <th scope="col" <%= jhiPrefix %>SortBy="createdDate">
            <span>__jhiTranslateTag__('userManagement.createdDate')</span> <fa-icon icon="sort" />
          </th>
          <th scope="col" <%= jhiPrefix %>SortBy="lastModifiedBy">
            <span>__jhiTranslateTag__('userManagement.lastModifiedBy')</span> <fa-icon icon="sort" />
          </th>
          <th scope="col" <%= jhiPrefix %>SortBy="lastModifiedDate">
            <span>__jhiTranslateTag__('userManagement.lastModifiedDate')</span> <fa-icon icon="sort" />
          </th>
<%_ } _%>
          <th scope="col"></th>
        </tr>
      </thead>
      <tbody>
      @for (userManagement of users(); track trackIdentity(userManagement)) {
        <tr data-cy="entityTable">
          <td>
            <a [routerLink]="['./', userManagement.login, 'view']">{{ userManagement.id }}</a>
          </td>
          <td>{{ userManagement.login }}</td>
          <td>{{ userManagement.email }}</td>
          <td>
          @if (!userManagement.activated) {
            <button class="btn btn-danger btn-sm"
              (click)="setActive(userManagement, true)"
            >__jhiTranslateTag__('userManagement.deactivated')</button>
          } @else {
            <button class="btn btn-success btn-sm"
              (click)="setActive(userManagement, false)"
              [disabled]="!currentAccount() || currentAccount()?.login === userManagement.login"
            >__jhiTranslateTag__('userManagement.activated')</button>
          }
          </td>
<%_ if (enableTranslation) { _%>
          <td>{{ userManagement.langKey }}</td>
<%_ } _%>
          <td>
          @for (authority of userManagement.authorities; track $index) {
            <div>
              <span class="badge bg-info">{{ authority }}</span>
            </div>
          }
          </td>
<%_ if (!locals.databaseTypeCassandra) { _%>
          <td>{{ userManagement.createdDate | date: 'dd/MM/yy HH:mm' }}</td>
          <td>{{ userManagement.lastModifiedBy }}</td>
          <td>{{ userManagement.lastModifiedDate | date: 'dd/MM/yy HH:mm' }}</td>
<%_ } _%>
          <td class="text-end">
            <div class="btn-group">
              <button type="submit" [routerLink]="['./', userManagement.login, 'view']" class="btn btn-info btn-sm" data-cy="entityDetailsButton">
                <fa-icon icon="eye" />
                <span class="d-none d-md-inline">__jhiTranslateTag__('entity.action.view')</span>
              </button>

              <button type="submit" [routerLink]="['./', userManagement.login, 'edit']" queryParamsHandling="merge" class="btn btn-primary btn-sm" data-cy="entityEditButton">
                <fa-icon icon="pencil-alt" />
                <span class="d-none d-md-inline">__jhiTranslateTag__('entity.action.edit')</span>
              </button>

              <button
                type="button"
                (click)="deleteUser(userManagement)"
                class="btn btn-danger btn-sm"
                data-cy="entityDeleteButton"
                [disabled]="!currentAccount() || currentAccount()?.login === userManagement.login"
              >
                <fa-icon icon="times" />
                <span class="d-none d-md-inline">__jhiTranslateTag__('entity.action.delete')</span>
              </button>
            </div>
          </td>
        </tr>
      }
      </tbody>
    </table>
  </div>
<%_ if (!locals.databaseTypeCassandra) { _%>

  <div>
    <div class="d-flex justify-content-center">
      <<%= jhiPrefixDashed %>-item-count [params]="{ page: page(), totalItems: totalItems(), itemsPerPage: itemsPerPage() }" />
    </div>

    <div class="d-flex justify-content-center">
      <ngb-pagination
        [collectionSize]="totalItems()"
        [(page)]="page"
        [pageSize]="itemsPerPage()"
        [maxSize]="5"
        [rotate]="true"
        [boundaryLinks]="true"
        (pageChange)="transition()"
      />
    </div>
  </div>
<%_ } _%>
}
</div>
