# ===================================================================
# Spring Cloud Config bootstrap configuration for the "aws" profile
# ===================================================================

spring:
    application:
        name: <%= temp.baseName %>
    cloud:
        config:
            fail-fast: true

cloud:
    aws:
        stack:
            name: <%= temp.baseName %>
            auto: false
