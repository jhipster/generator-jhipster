Description:
    Creates all required rancher-compose deployment configuration for the selected applications.

Example:
    yo jhipster:rancher-compose

    This will create:
        [application]/docker-compose.yml
        [application]/rancher-compose.yml
