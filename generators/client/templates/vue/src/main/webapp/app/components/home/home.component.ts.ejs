import Component, { mixins } from 'vue-class-component';
import { mapGetters } from 'vuex';
import LoginModalService from '@/components/account/LoginModalService.vue';
import Principal from '@/components/account/principal';

@Component({
  computed: mapGetters(['authenticated', 'account'])
})
export default class Home extends mixins(LoginModalService, Principal) {}
