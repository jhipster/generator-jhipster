import axios, { AxiosInstance } from 'axios';

export default class RegisterService {
  private axios: AxiosInstance;

  constructor() {
    this.axios = axios;
  }

  public processRegistration(account: any): Promise<any> {
    return this.axios.post('api/register', account);
  }
}
