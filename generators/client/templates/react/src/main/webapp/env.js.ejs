/*
The code below creates a global __env variable if not already declared,
it can be accessed in you angular file but we recommend exposing it through constants.ts .
When using kubernetes, this file (with no comments) can be mounted as a configMap, this is why we keep such a simple key value syntax
*/
window.__env = window.__env || {};
// window.__env.myDynamicVariable = 'http://127.0.0.1:8090';
