import React from 'react';
import { Alert } from 'react-bootstrap';

const ErrorLoading = () => {
  return (
    <div>
      <Alert variant="danger">Error loading component</Alert>
    </div>
  );
};

export default ErrorLoading;
