<%
var classes = [];
var paths = [];
var hasUser = false;
for (idx in differentRelationships) {
    if (differentRelationships[idx].otherEntityNameCapitalized != 'User') {
        classes.push(differentRelationships[idx].otherEntityNameCapitalized);
        paths.push(differentRelationships[idx].otherEntityStateName); 
    } else {
        hasUser = true;
    }
}
if (classes.length > 0) {
   for (idx in classes) { %>
import { <%= classes[idx] %>, <%= classes[idx] %>Service } from '../<%= paths[idx] %>/';
<% }
}
if (hasUser) { %>
import { User, UserService } from '../../admin/';
<%  } %>
